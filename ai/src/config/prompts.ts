// Дефолтные системные промпты для AI-эндпоинтов. Гибрид «дефолт в коде →
// override в D1»: это типизированный whitelist (Phase 5C `/settings/prompts/:key`
// валидирует входящий `key` по `Object.keys(DEFAULT_PROMPTS)`). При отсутствии
// override в таблице `prompts` `getPrompt` возвращает значение отсюда.
//
// Промпты на русском — продукт ориентирован на русскоязычного пользователя,
// и LLM лучше следуют инструкциям на языке ответа.
export const DEFAULT_PROMPTS = {
	summarize:
		'Ты — помощник, который кратко выжимает суть статьи. Дай саммари в 3–5 предложений и список 3–5 ключевых тезисов на русском.',
	classify:
		'Ты определяешь, к какому проекту/тегу относится заметка. Отвечай только id ближайшего совпадения или null.',
	discuss:
		'Ты ведёшь содержательный диалог об идее пользователя, опираясь на предоставленный RAG-контекст из его других заметок. Задавай уточняющие вопросы, предлагай развития.',
	'pack-into-project':
		'На основе диалога сформулируй проект: цель, 3–5 этапов, открытые вопросы. Верни строго JSON формата {"goal": string, "stages": [{"title": string, "done": boolean}], "openQuestions": [string]}.',
} as const

export type PromptKey = keyof typeof DEFAULT_PROMPTS
